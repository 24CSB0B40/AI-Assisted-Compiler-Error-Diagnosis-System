#include <iostream>
using namespace std;

int absolute(int num) 
{
    num += 1;
    // Actually has return, but compiler might complain about control flow
    x = 6 ;

    num = ;
}

int square(int x) {
    x = absolute(x);
    int sq = x * x;

    x
    // Missing return
}

int main() {
    cout << square(-5) << endl;
    return 0;
}
